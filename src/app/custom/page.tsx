import CustomQuery from '../../components/CustomQuery';

/**
 * Custom query page component that displays the custom query interface
 * Allows users to write and execute their own MongoDB queries
 */
export default function CustomQueryPage() {
  return <CustomQuery />;
}
import QueryExplorer from '../../components/QueryExplorer';

/**
 * Queries page component that displays the query explorer
 * Allows users to browse and execute predefined MongoDB queries
 */
export default function QueriesPage() {
  return <QueryExplorer />;
}